<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nopecsnosex">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoman"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserPassActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BMIActivity" />
        <activity android:name=".AchievementsActivity" />
        <activity android:name=".MainPageActivity" />
        <activity android:name=".FinalNextActivity" />
        <activity android:name=".HeightWeightActivity" />
        <activity android:name=".NameAgeActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>